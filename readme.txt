This projects compresses an ASCII text file using Huffman coding 

dataCompression.py takes in one argument, this being a file location. A list of example files can be located in the folder “Data compression examples”. If no argument is given, the user 
will be asked to enter a file location.

To run this project python3 is required
